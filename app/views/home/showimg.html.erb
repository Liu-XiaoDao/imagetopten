	
	<div style="text-align: center;"><h1></h1></div>
	<article class="jq22-container">
		<div class="content">
			<div class="chroma-gallery mygallery">
				<% @images.each do |image| %>
					<img src="<%= asset_path(image.url) %>" alt="<%= image.title %>" bt="<button type='button' class='btn' id='myBtn'>Click Me!</button>" data-largesrc="<%= asset_path(image.url) %>">
				<% end %>

			</div>
		</div>
	
	</article>
	<style type="text/css">
		.btn{
			color: red
		}
	</style>
	<button type='button' class='btn' id='myBtn'>Click Me!</button>






